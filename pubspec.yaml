name: supertokens_dart_backend
description: A server app using the shelf package and Docker.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.2.3

dependencies:
  args: ^2.4.0
  http: ^1.1.2
  shelf: ^1.4.0
  shelf_router: ^1.1.0

dev_dependencies:
  lints: ^2.1.0
  test: ^1.24.0
